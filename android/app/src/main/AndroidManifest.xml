<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Layer 1 + 2: Accessibility Service permissions -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:allowBackup="true"
        android:label="KavachVoice"
        android:theme="@style/Theme.KavachVoice">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Layer 1 + 2: Accessibility Service -->
        <service
            android:name=".KavachAccessibilityService"
            android:exported="true"
            android:label="KavachVoice Protection"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Keeps service alive in background -->
        <service
            android:name=".KavachForegroundService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- Restarts foreground service if killed -->
        <receiver
            android:name=".ServiceRestartReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.kavachvoice.RESTART_SERVICE" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
